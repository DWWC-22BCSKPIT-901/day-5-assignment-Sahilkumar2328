#include <iostream>
#include <vector>
using namespace std;
int main() {
 int n, k, x;
 cin >> n >> k;
 vector<int> arr;
 for (int i = 0; i < n; i++) {
 cin >> x;
 arr.push_back(x);
 }
 for (int i = 0; i < n; i++) {
 if (arr[i] == k) {
 cout << i + 1;
 return 0;
 }
 }
 cout << -1;
 return 0;
}
